const is = require('lib/is');

if (is.str('user')) {
  console.log('user');
}

module.exports = {
  load: observables => observables.videoId.set('z0J8LHXo6bw'),
  render: () => {
    return 'wesh';
  }
}